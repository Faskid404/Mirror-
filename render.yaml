services:
    - type: web
      name: mirror
      runtime: python
      buildCommand: pip install -r scanner/requirements.txt && cd artifacts/vulnscan && npm install && npm run build
      startCommand: cd scanner && gunicorn app:app
      envVars:
        - key: NODE_ENV
          value: production
  